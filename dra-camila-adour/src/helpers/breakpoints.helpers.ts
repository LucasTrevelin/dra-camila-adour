export const breakpoints = {
  sm: '640px',
  lg: '1024px',
  lg1: '1300px'
}

export const devices = {
  sm: `(max-width: ${breakpoints.sm})`,
  lg: `(max-width: ${breakpoints.lg})`,
  lg1: `(max-width: ${breakpoints.lg1})`
}
